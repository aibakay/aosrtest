# Issues Backlog

Составлено по результатам полного code review (2026-07-01). Работаем по этому списку последовательно, отмечая статус по мере исправления.

Легенда статуса: `[ ]` не начато · `[~]` в работе · `[x]` исправлено

---

## Critical

- [x] **C1. Публичный API без авторизации.**
  Backend на Railway открыт всему интернету: любой может читать/изменять/удалять
  справочник приказов (ФИО, должности — ПДн) и реестры актов, нагружать генерацию.
  `cors()` без ограничений усугубляет.
  **Файлы:** `backend/src/server.ts`
  **Исправление:** API-ключ middleware (заголовок, env `API_KEY`) + CORS-whitelist на
  домен Vercel + rate-limit на `/generate`.
  **Сложность:** средняя.
  **Статус:** сделано. `backend/src/middleware/apiKey.ts` — гейт по заголовку
  `x-api-key` против env `API_KEY` (no-op, если `API_KEY` не задан — не ломает
  локальную разработку); `/api/health` всегда открыт (для health-check платформы).
  `backend/src/app.ts` — CORS теперь настраивается через `ALLOWED_ORIGINS`
  (comma-separated), по умолчанию (не задан) разрешён любой Origin.
  `backend/src/middleware/rateLimiters.ts` (express-rate-limit) — общий лимит
  300/15мин на весь `/api`, отдельный лимит 60/15мин на `/api/documents/generate`
  и `/api/registries/:id/generate`. Вынесен `backend/src/app.ts::createApp()`
  отдельно от `server.ts` для тестируемости (уже сделано в рамках H1).
  Фронтенд: `frontend/src/api/config.ts::apiFetch()` — централизованная обёртка
  над `fetch`, добавляет `x-api-key` из `VITE_API_KEY`, если задан; все вызовы в
  `client.ts`/`registries.ts`/`orderDirectives.ts` переведены на неё.
  Задокументировано в README (раздел «Защита API (продакшн)») и
  `frontend/.env.example`. Добавлены тесты `middleware/apiKey.test.ts` (5 тестов:
  open-by-default, 401 без ключа/с неверным ключом, 200 с верным ключом,
  health всегда открыт) — итого 42 backend-теста проходят.
  **Ограничение (осознанное):** общий API-ключ в бандле фронтенда — защита от
  ботов/сканеров публичного URL, не полноценная авторизация пользователей.

---

## High

- [~] **H1. Ноль тестов.**
  Ни в одном package.json нет test-скрипта. Ядро (`bookmarkFiller`, `generatorService`,
  `splitDate`, `isInForceOn`, `parseAttachments`) — чистые функции, идеально тестируемые,
  но рефакторинг сейчас "вслепую".
  **Исправление:** vitest — unit-тесты ядра + supertest integration на `/generate`.
  **Сложность:** средняя.
  **Статус:** backend покрыт — `npm test` в `backend/` (vitest), 37 тестов, 5 файлов:
  `bookmarkFiller.test.ts`, `templateService.test.ts` (splitDate, включая
  timezone-регрессию), `qualityRegistryService.test.ts` (parseAttachments,
  attachmentBookmarkKey, generateQualityRegistry), `orderDirectiveService.test.ts`
  (isInForceOn, validateInput), `routes/documents.integration.test.ts` (supertest —
  health/templates/generate, включая реальную генерацию .docx для всех 15 шаблонов).
  Для supertest вынесен `backend/src/app.ts` (createApp()) отдельно от `server.ts`
  (app.listen), чтобы тестировать без реального порта.
  Тесты `getActiveOn`/CRUD-роутов (registries, order-directives) намеренно не
  добавлены — они читают/пишут реальные JSON-файлы в `backend/data/`, для них
  нужна инъекция репозитория с временным файлом (не сделано).
  Frontend тестами пока не покрыт — нет vitest/testing-library в
  `frontend/package.json`.

- [x] **H2. Тихие отказы в `bookmarkFiller.ts`.**
  Если закладка — "точка" без `<w:t>` внутри, или `bookmarkEnd` сериализован иначе
  (с пробелом), значение молча выбрасывается — документ уходит с пустым/старым текстом,
  никто не узнаёт. Заменяется только первый `<w:t>`, остальной текст между start/end
  остаётся.
  **Файл:** `backend/src/services/bookmarkFiller.ts`
  **Исправление:** возвращать список незаполненных ключей, обрабатывать закладку-точку
  вставкой рана. Делать только после появления тестов (H1).
  **Сложность:** средняя.
  **Статус:** сделано. `fillBookmarks` теперь возвращает `{ xml, unresolved }` вместо
  голой строки — точечные закладки (start/end без `<w:t>` внутри) больше не теряют
  значение: вставляется новый `<w:r>` перед `bookmarkEnd`. `generatorService.ts`
  собирает `unresolvedBookmarks`, логирует `console.warn` и возвращает список в
  `GenerateResult`, отфильтровав два намеренно-неполных источника ложных
  срабатываний: аддитивные ключи `Приказ_*` (нет закладки в шаблонах — см.
  `docs/TEMPLATES.md`) и виртуальные `*_picker`-поля дат (никогда не бывают
  закладками, только источник для сплита день/месяц/год). Оба фильтра выявлены и
  закрыты через тесты (без них тест на реальный шаблон АОСР падал бы ложно).
  `routes/documents.ts` шлёт `X-Unresolved-Bookmarks` (только когда список
  непустой), CORS `exposedHeaders` обновлён, чтобы фронт мог прочитать заголовок
  кросс-origin (заодно поправлен тот же баг для уже существовавшего
  `Content-Disposition`, который в проде — Vercel/Railway разные origin — не
  читался, хотя в локальной разработке через Vite-прокси работал). Фронт
  (`api/client.ts`, `DocumentForm.tsx`) показывает жёлтое предупреждение со
  списком человекочитаемых меток полей, если что-то не влезло, но всё равно даёт
  документ скачаться.
  Тесты: `bookmarkFiller.test.ts` (точечная закладка, многострочная точечная
  закладка, unresolved-репортинг), `generatorService.test.ts` (unresolved пуст на
  полностью заполненном шаблоне, аддитивные `Приказ_*` не всплывают, настоящий
  незнакомый ключ — всплывает), плюс `documents.integration.test.ts` проверяет
  отсутствие `X-Unresolved-Bookmarks` на всех 15 реальных шаблонах. E2E проверено
  вручную через Playwright (заполнение формы АОСР → генерация → скриншот без
  предупреждения).

- [x] **H3. Коллизия черновиков localStorage.**
  `DocumentForm` пишет черновик под ключом `draft:<templateCode>` всегда — в том числе
  в режиме редактирования акта реестра (`onSave`). Редактирование акта АОСР в реестре
  молча перезаписывает черновик страницы "Генератор ИД" для АОСР.
  **Файл:** `frontend/src/components/DocumentForm.tsx:70-72`
  **Исправление:** не писать черновик в режиме `onSave`, либо использовать ключ вида
  `draft:registry:<actId>`.
  **Сложность:** низкая.

- [x] **H4. Батч-генерация реестра — "всё или ничего" без деталей.**
  Если один акт из N невалиден (нет шаблона, битые данные), весь ZIP падает с общим
  "Ошибка генерации реестра" без указания виновного акта. Валидация обязательных полей
  акта при сохранении в реестр не выполняется — узнаётся только при генерации.
  **Файл:** `backend/src/routes/registries.ts:111-150`
  **Исправление:** try/catch на каждый акт, собирать `{actId, error}[]`, вернуть 422 со
  списком; валидировать акт при сохранении.
  **Сложность:** средняя.
  **Статус:** сделано.
  - `POST /:id/acts` и `PUT /:id/acts/:actId` теперь прогоняют объединённые данные
    акта через `validationService.validate()` (тот же валидатор, что и обычная
    генерация) и возвращают 400 с `errors[]`, если не хватает обязательных полей —
    акт с дырами больше нельзя сохранить в реестр.
  - `POST /:id/generate` генерирует каждый акт в своём `try/catch`; сломанный акт
    не валит всю пачку. Если провалились все акты — 422 с `failures: {actId,
    templateCode, error}[]`. Если провалилась часть — ZIP всё равно собирается из
    успешных актов, а список неудач уходит в заголовок `X-Registry-Warnings`
    (добавлен в CORS `exposedHeaders`).
  - Фронт (`api/registries.ts::generateRegistry`, `RegistryPage.handleGenerateAll`)
    читает `failures`/заголовок и показывает toast с числом скачанных актов и
    списком проваленных типов документов, не блокируя скачивание успешных.
  - Тесты (`registries.integration.test.ts`, 4 теста): валидация при
    добавлении/редактировании акта (пусто → 400, заполнено → 201/200), 422 когда
    все акты сломаны, 200+заголовок+частичный ZIP когда сломан только один акт из
    двух. Тесты сами создают и удаляют временный реестр — `backend/data/
    registries.json` не засоряется (проверено вручную после прогона).

---

## Medium

- [x] **M1. Шапка не адаптивна.**
  `Header` + `NavTabs`: 4 таба с текстом + длинный заголовок в одну строку без
  переноса/бургера — на телефоне гарантированное переполнение по горизонтали.
  **Файлы:** `frontend/src/components/layout/Header.tsx`, `NavTabs.tsx`
  **Исправление:** `flex-wrap` + скрытие подписей табов на `sm:` (иконки + `aria-label`),
  либо нижняя навигация.
  **Сложность:** низкая.
  **Статус:** сделано. `NavTabs` — подписи скрыты ниже `sm:` (остаются иконки +
  `aria-label`/`title`), `flex-wrap` на случай узкого экрана. `Header` — заголовок
  сокращается до "Генератор ИД" и подзаголовок скрывается ниже `sm:`, `min-w-0`/
  `truncate` вместо жёсткого переполнения. `Link` (`router.tsx`) расширен, чтобы
  принимать `aria-label`/`title`. Проверено Playwright-скриншотами на 375/768/1280px —
  `document.documentElement.scrollWidth > clientWidth` = false на всех трёх.

- [~] **M2. Дублирование и рассинхронизация логики.**
  Санитизация имени файла реестра существует в двух местах с *разными* регэкспами:
  `RegistryPage.tsx:129` (`А-яЁё`) и `registries.ts:136` (`Ѐ-ӿ`). SVG-иконки копипастятся
  по страницам (одна и та же "стрелка вниз" определена трижды). В `generatorService.ts`
  два подряд `if (attachments.length > REGISTRY_THRESHOLD)` и дважды вычисляется
  `safeName`.
  **Файлы:** `frontend/src/pages/RegistryPage.tsx`, `backend/src/routes/registries.ts`,
  `backend/src/services/generatorService.ts`
  **Исправление:** имя файла брать только из `Content-Disposition` (уже парсится в
  `client.ts`); вынести общие иконки в отдельный файл; убрать дублирующий if/safeName.
  **Сложность:** низкая.
  **Статус:** сделано полностью. filename mismatch и дублирующий if/safeName в
  generatorService исправлены (`api/registries.ts::generateRegistry` теперь парсит
  `Content-Disposition`, как `client.ts`). Общие SVG-иконки вынесены в
  `frontend/src/components/icons.tsx` (`DownloadIcon`, `PlusIcon`, `BookIcon`,
  `DocIcon`, `ListIcon`, `EmptyDocOutlineIcon`) и переиспользованы вместо 6 пар
  дублировавшихся инлайн-SVG в `App.tsx`, `DashboardPage.tsx`, `RegistriesPage.tsx`,
  `RegistryPage.tsx`, `DocumentForm.tsx`. Проверено Playwright-скриншотами
  (dashboard/registries) — иконки визуально идентичны прежним, консоль чистая.

- [x] **M3. Расхождение с CLAUDE.md — не сохраняется в `/backend/output`.**
  Требование "save generated files to /backend/output" больше не выполняется
  (коммит 9cb8dba перевёл на стриминг буфера); в `backend/output` лежат устаревшие файлы.
  **Файл:** `backend/src/services/generatorService.ts`
  **Исправление:** либо вернуть сохранение копий в output (+ очистка старых), либо явно
  задокументировать отклонение в docs.
  **Сложность:** низкая.
  **Статус:** сделано — вернули сохранение. `saveToOutput()` пишет копию каждого
  сгенерированного `.docx` (основной документ + реестр качества, если есть) в
  `backend/output`, best-effort (ошибка записи логируется, но не мешает ответу
  пользователю). Пропускается при `NODE_ENV=test`, чтобы тесты не заваливали
  директорию файлами на каждый прогон — проверено: количество файлов в `output/`
  не изменилось после `npm test` (52 теста генерируют документы). Проверено вручную
  через реальный dev-сервер: POST на `/api/documents/generate` действительно
  создал новый файл в `backend/output`. Очистку старых файлов не добавляли — не
  было явного требования, `output/` в `.gitignore`, так что рост директории не
  просачивается в репозиторий.

- [x] **M4. Дыры валидации CRUD.**
  `PUT /api/registries/:id` принимает пустой `name`; `POST /:id/acts` не проверяет
  существование `templateCode`; кэш шаблонов `_cache` не сбрасывается никогда (изменение
  шаблона требует рестарта — нигде не задокументировано).
  **Файлы:** `backend/src/routes/registries.ts`, `backend/src/services/templateService.ts`
  **Исправление:** проверять непустой name на PUT, существование templateCode на
  добавлении акта.
  **Сложность:** низкая.
  **Статус:** name/templateCode валидация добавлена в `registries.ts` (PUT registry,
  POST/PUT acts). Инвалидация кэша шаблонов `_cache` — отдельный пункт, не тронут
  (требует рестарта процесса при замене .docx-шаблона, задокументировано здесь).

- [x] **M5. Предупреждения линтера.**
  Отсутствующая зависимость `load` в `useEffect` (`RegistryPage.tsx:81`),
  fast-refresh-предупреждения в `router.tsx` и `Toast.tsx`.
  **Исправление:** `useCallback`/перенос `load` внутрь эффекта.
  **Сложность:** низкая.

---

## Low

- [x] **L1. Неиспользуемый hero.png.**
  `frontend/src/assets/hero.png` (16 КБ) нигде не используется.
  **Исправление:** удалить.
  **Сложность:** низкая.

- [x] **L2. Хардкод-списки порядка размазаны по фронту и бэку.**
  `ORDER` (templateService) и `GROUP_ORDER` (DocumentForm) — знание о доменном порядке
  дублируется на фронте и бэке.
  **Сложность:** низкая.
  **Статус:** при разборе оказалось, что это не настоящее дублирование одних и тех
  же данных — `ORDER` управляет порядком типов документов (`/api/templates`),
  `GROUP_ORDER` управляет порядком секций полей внутри одного шаблона на фронте;
  это два независимых списка, которым не нужно совпадать (frontend больше нигде не
  хранит свою копию `ORDER` — проверено, `TemplateSelector.tsx` просто рендерит
  порядок, пришедший от backend). Добавлены комментарии в обоих файлах,
  объясняющие это и явно ссылающиеся друг на друга, чтобы будущий читатель не
  тратил время на то же самое разбирательство.

- [x] **L3. Доступность.**
  Кнопки-иконки (`BackIcon`, `CloseIcon`) имеют `title`, но не `aria-label`;
  фокус-ловушка в `ConfirmDialog` не проверена.
  **Сложность:** низкая.
  **Статус:** сделано. Добавлен `aria-label` (и `title`, где отсутствовал) на:
  `BackIcon`/оба `CloseIcon` в `RegistryPage.tsx`, три кнопки-глифа
  (▲/▼/✕ — "Переместить вверх/вниз"/"Удалить приложение") в
  `AttachmentsListField.tsx`, кнопку ✕ в `OrderDirectiveForm.tsx`.
  `ConfirmDialog.tsx` переписан: `role="alertdialog"`, `aria-modal="true"`,
  `aria-labelledby`, автофокус на диалог при открытии, закрытие по `Escape`,
  циклический focus-trap по `Tab`/`Shift+Tab` внутри диалога (раньше фокус мог уйти
  на страницу под затемнением). Проверено Playwright: диалог получает фокус при
  открытии, `Escape` закрывает, `aria-modal="true"` присутствует, консоль чистая.

- [x] **L4. Нет логирования/helmet.**
  Нет request-логирования (morgan/pino) и helmet — при инцидентах на Railway
  диагностировать нечем.
  **Файл:** `backend/src/server.ts`
  **Исправление:** добавить pino-http (или morgan) + helmet.
  **Сложность:** низкая.
  **Статус:** сделано в `backend/src/app.ts`. `pino-http` логирует каждый запрос
  (отключено при `NODE_ENV=test`, чтобы не шуметь в выводе vitest). `helmet()`
  с явным `crossOriginResourcePolicy: { policy: "cross-origin" }` — дефолтный
  `same-origin` CORP сломал бы скачивание документов, так как frontend (Vercel)
  и backend (Railway) на разных origin; браузер блокирует такие ответы
  независимо от CORS-заголовков. Проверено вручную: `curl -D -` показывает
  `Cross-Origin-Resource-Policy: cross-origin`, и Playwright end-to-end тест
  (заполнение формы АОСР в реальном браузере → клик «Сформировать документ» →
  реальное скачивание .docx) прошёл без console-ошибок.

- [ ] **L5. `source/` раздувает репозиторий.**
  xlsm, zip, pdf в git — допустимо по требованиям CLAUDE.md, но стоит рассмотреть
  git-lfs при росте.
  **Сложность:** низкая.
  **Статус:** сознательно не трогали. В отличие от остальных пунктов это не
  просто правка кода — включение git-lfs означает переписывание истории
  (`git lfs migrate`) или как минимум новую политику для будущих больших файлов,
  плюс зависимость от LFS-квоты на удалённом хостинге (GitHub и т.п.). Это
  затрагивает то, как репозиторий работает для всех, у кого есть клон, и
  потенциально требует переписывания истории — решение, которое должен принять
  пользователь, а не быть тихо сделано в фоне мелких правок.

---

## План работы

**Быстрые правки (1–2 часа):** ✅ H3, M2, M4, M5, L1.

**Улучшения на 1 день:** ✅ C1, H1 (backend; frontend не покрыт), M1, L4.

**Крупные доработки:** ✅ H2, H4.

**Мелкие хвосты:** ✅ M3, L2, L3.

**Осталось:**
- **L5** (git-lfs для `source/`) — сознательно не сделано, требует решения
  пользователя (переписывание истории/LFS-квота на хостинге).
- Тесты фронтенда (vitest/testing-library ещё не подключены — см. H1).
- PDF-экспорт, инвалидация кэша шаблонов (`templateService._cache`),
  e2e-тесты для frontend — из раздела "в последнюю очередь", не начаты.
