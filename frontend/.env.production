# Backend API base URL used for `vite build` (production).
# Overridable by a platform-level VITE_API_URL env var (e.g. in Vercel).
VITE_API_URL=https://jubilant-unity-production-cdbb.up.railway.app/api
