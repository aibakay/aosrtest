{
  "name": "docx-autogenerator",
  "version": "1.0.0",
  "private": true,
  "description": "Web replacement for the Excel/VBA document autogenerator. Frontend (Vite/React) served as static assets, backend (Express) deployed as a Vercel serverless function under /api.",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pizzip": "^3.1.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
